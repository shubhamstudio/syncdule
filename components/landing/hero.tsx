import Link from "next/link";
import { Check, Send } from "lucide-react";
import { HugeiconsIcon } from "@hugeicons/react";
import { getChannelIcon } from "@/constants/channels";
import { landingChannels } from "./landing-data";
export function Hero({ isSignedIn }: { isSignedIn: boolean | undefined }) { return <section className="landing-hero" aria-labelledby="landing-title"><div className="landing-hero-glow" aria-hidden="true" /><div className="landing-container landing-hero-grid"><div className="landing-hero-copy"><p className="landing-eyebrow"><span aria-hidden="true" />Social publishing, made deliberate</p><h1 id="landing-title">Make your content move <em>with purpose.</em></h1><p className="landing-hero-description">Build, tailor, and schedule the next version of your brand across every channel—without losing your creative pace.</p><div className="landing-actions"><Link href={isSignedIn ? "/schedule" : "/sign-up"} className="landing-button landing-button-primary">{isSignedIn ? "Open workspace" : "Start creating"}<Send aria-hidden="true" className="size-4" /></Link><a href="#how-it-works" className="landing-button landing-button-secondary">See how it works</a></div><p className="landing-hero-proof">Free workspace <span aria-hidden="true">·</span> Four channels <span aria-hidden="true">·</span> AI assisted</p></div><div className="landing-preview" aria-label="FlashSync calendar preview"><div className="landing-preview-main"><div className="landing-preview-heading"><div><p>Campaign pulse</p><strong>September rhythm</strong></div><span>On track</span></div><div className="landing-preview-week">{["M", "T", "W", "T", "F", "S", "S"].map((day, index) => <div className={index === 3 ? "is-active" : ""} key={`${day}-${index}`}><small>{day}</small><b>{16 + index}</b></div>)}</div><div className="landing-preview-posts"><div><span className="landing-channel-mark linkedin">in</span><p><b>The launch story</b><small>Thursday · 10:00 AM</small></p><Check aria-label="Scheduled" /></div><div><span className="landing-channel-mark instagram">◎</span><p><b>Behind the scenes</b><small>Friday · 06:30 PM</small></p><i aria-label="Draft pending" /></div></div></div><div className="landing-preview-stats"><div><Send aria-hidden="true" /><p>Scheduled posts</p><strong>12</strong><span className="landing-chart" aria-hidden="true">{[35,62,46,83,55,72,91,68].map((height, index) => <i key={index} style={{ height: `${height}%` }} />)}</span></div><div><p>Connected</p><strong>4 <small>channels</small></strong><span className="landing-channel-grid">{landingChannels.map((channel) => { const icon = getChannelIcon(channel.type); return <span className={channel.token} key={channel.type}>{icon && <HugeiconsIcon icon={icon} aria-label={channel.name} />}</span>; })}</span></div></div></div></div></section>; }
